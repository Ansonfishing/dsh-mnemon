import type { HostContextShape } from './contracts.ts';
export declare const GUIDANCE_SECTION_NAME = "mnemon:routing";
export declare const ROUTING_GUIDANCE = "Decide whether durable context could materially improve the turn. If so, call mnemon_recall with a focused query; otherwise continue without recalling. Treat returned evidence as fallible, and trust a memory write only after its tool receipt confirms it.";
export declare function registerGuidance(ctx: HostContextShape): void;
//# sourceMappingURL=guidance.d.ts.map